name: Deploy Dashboard to Cloudflare Pages

on:
  # GITHUB_TOKEN 経由の auto-merge では push イベントが他ワークフローに伝播しないため、
  # PR close (= merged) イベントをトリガーとして使用する。main は保護ブランチのため
  # 直接 push は不可であり、PR merge がデプロイの唯一の経路となる。
  pull_request:
    types: [closed]
    branches: [main]
    paths:
      - "dashboard/**"
  workflow_dispatch:

jobs:
  deploy:
    # PR close の場合は merged のときのみ実行
    if: github.event_name != 'pull_request' || github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Python deps
        run: pip install -e ".[dev]"

      - uses: pnpm/action-setup@v6
        with:
          version: "11.6.0"

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: "pnpm"
          cache-dependency-path: dashboard/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        working-directory: dashboard

      - name: Build
        run: pnpm build
        working-directory: dashboard

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy out --project-name=tech-article-fetcher-dashboard
          workingDirectory: dashboard
